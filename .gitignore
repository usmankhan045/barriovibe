node_modules
.next
out
build
.DS_Store
*.pem

.env
.env*.local

.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Source mockups — inputs to `pnpm art`, never shipped to the browser.
# Keep them in version control so the art pipeline is reproducible.
!assets/source
